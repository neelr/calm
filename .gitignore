_metadata/
.DS_Store

# Local Curius JWT for curl / debugging (never commit)
curius/.curius-token

# Local OpenAI key for MangaDex translation (never commit)
mangadex/local-config.json
mangadex/nippon-sangoku-context.html

_locales/
locales/

# Local Papery instance URL (host stays out of the repo; never commit)
papery/local-config.json
